import { ViewCart } from "../components/ShoppingCart";

export default function Checkout(props) {
    return (
        <>
            Checkout
            <ViewCart className="w-52"/>
        </>
    );
}